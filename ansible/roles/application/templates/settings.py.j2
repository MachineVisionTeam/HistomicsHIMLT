"""
Auto-generated settings.py for HIMLT
Generated by Ansible deployment on {{ ansible_date_time.iso8601 }}
"""

import os
from pathlib import Path

# Base directory
BASE_DIR = Path("{{ app_install_dir }}")

# Database configuration
DATABASE = {
    'host': "{{ db_host }}",
    'port': {{ db_port }},
    'database': "{{ db_name }}",
    'user': "{{ db_user }}",
    'password': "{{ db_password }}",
}

# Redis configuration
REDIS = {
    'host': "{{ redis_host }}",
    'port': {{ redis_port }},
}

# Paths - Simple!
CHECKPOINT_DIR = Path("{{ checkpoints_dir }}")

# ML configuration
ML_CONFIG = {
    'default_classifier': 'RandomForest',
    'random_state': 42,
    'n_jobs': -1,
}
